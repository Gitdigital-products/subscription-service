{
  "name": "@gitdigital/subscription-service",
  "version": "1.0.0",
  "description": "Federated Hub for GitDigital Products, Tiers, and Safety Gates",
  "private": true,
  "engines": {
    "node": ">=24.13.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "nest start --watch",
    "build": "nest build",
    "test": "jest",
    "mirror": "node scripts/trigger-mirror.js",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "dependencies": {
    "@solana/kit": "^2.0.0",
    "@solana/spl-token": "^0.4.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@prisma/client": "^6.0.0",
    "stripe": "^14.0.0",
    "redis": "^4.6.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.3.0",
    "prisma": "^6.0.0",
    "eslint": "^9.0.0"
  }
}
